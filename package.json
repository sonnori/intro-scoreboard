{
  "name": "intro-scoreboard",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "app": "SINGLE_FILE=1 vite build && node scripts/bundle.mjs && node scripts/make-app.mjs && vite build",
    "serve": "node server/index.mjs",
    "start": "vite build && node server/index.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "@fontsource-variable/anuphan": "^5.3.0",
    "@fontsource/chakra-petch": "^5.3.0",
    "three": "^0.180.0"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  }
}
